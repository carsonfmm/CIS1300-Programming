#include <stdio.h>

main()
{
   int num;
   int i;
   int num_twins = 0;
   for ( num = 2; num < 3000; num++ ) {
      if ( i = 2; i <= num; i++ ) {

      }
      else {
         num_twins++;
      }
   }

printf("%d", num_twins);

}